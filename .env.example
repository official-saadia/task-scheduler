# Copy to .env at the repo root. docker compose reads it automatically.
# Every value has a working default, so `docker compose up` works without this file.

MYSQL_ROOT_PASSWORD=root

# Generate your own before putting this anywhere reachable:
#   openssl rand -hex 32
JWT_SECRET=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970

ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
